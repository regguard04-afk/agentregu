// settings.js — Central configuration file
// ==========================================
// This is the ONLY file you need to change if the backend URL changes.
// All API calls read from this config.

const CONFIG = {
  apiBaseUrl: "https://regulatory-agent-backend.onrender.com"
};

export default CONFIG;
